use std::collections::HashMap;

fn main() {
    let l: Vec<i32> = vec![1, 2];
    let m: HashMap<i32, String> = HashMap::from([
        (1, "one".to_string()),
        (2, "two".to_string())
    ]);
}
