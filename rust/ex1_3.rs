fn is_positive(num: i32) -> bool { num > 0 }

fn print_string(str: String) -> () {}

fn add(num1: i32, num2: i32) -> i32 { num1 + num2 }

fn main() {
    let res: i32;
    res = 3;

    add(1, 2);
    is_positive(1);
    print_string("2".to_string());
}
