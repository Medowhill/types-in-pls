let res: number;
res = 3;

function isPositive(num: number): boolean { return num > 0; }
isPositive(1);

function printString(str: string): void {}
printString("2");

function add(num1: number, num2: number): number { return num1 + num2; }
add(1, 2);
